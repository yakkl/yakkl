# Configuration for making Yakkl app frontends accept the
# X-Forwarded-For header used by the Yakkl proxy.  The accepted IP
# addresses here are set in /etc/yakkl/yakkl.conf.
real_ip_header X-Forwarded-For;
<% @loadbalancers.each do |host| -%>
set_real_ip_from <%= host %>;
<%
end
-%>
